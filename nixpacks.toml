[phases.setup]
nixPkgs = ["python311", "python311Packages.pip", "nodejs_20"]

[phases.install]
cmds = ["npm install", "pip install --upgrade pip", "pip install -r requirements.txt"]

[start]
cmd = "npm start"